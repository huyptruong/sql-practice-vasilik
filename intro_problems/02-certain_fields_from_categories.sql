/*
River formatting: Spaces may be used to line up the code so that the root keywords all end on the same character boundary.
*/
select c.CategoryName as category_name
     , c.Description as description
  from Categories as c